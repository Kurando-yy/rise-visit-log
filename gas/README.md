# ライズ来店記録 GAS — このディレクトリの読み方

★**運用（修正・編集・適用）はマリア。** 大神は「原因と直し方を渡す」側。
（司令決定 2026-08-14 msg `1537678816519127071`。詳細＝`Obsidian/01_agents/01_for_agents/20260814_大神とマリアの棲み分け_大神.md`）

## ファイル

| ファイル | 中身 | 扱い |
|---|---|---|
| `Code_production.gs` | ★**本番の控え。毎晩 23:55 に自動で写される** | これを読む |
| `Code.gs` | 2026-08-10 に大神が書いた**受け口だけ**の初期版 | ★**本番ではない** |
| `Code.gs.bak_long_20260810` | 同上の旧版 | 参考のみ |
| `Code_production_20260814.gs.old` | 8/14 時点の控え（手で置いたもの） | 参考のみ |
| `patch_datekey_20260814.md` | 8/14 にマリアへ渡した修正内容の説明 | 適用済み |

★**`Code.gs` を本番だと思って読まないこと。**
`dailyRollup` / `morningCheck` / `tapZeroCheck` / `analyticsRollup` / `todayList_` などは `Code.gs` に**存在しない**。

## 更新の流れ（人が思い出さなくてよい形にしてある）

```
23:01  集計（マリアのトリガー）
23:50  ★マリアの自動書き出し → Obsidian 08_CUT/ライズ南関町/ライズ来店記録GAS_本番ソース全文.md
23:55  ★大神の突合ジョブ（com.oogami.rise-crosscheck）が、そのコード部分をここへ写す
```
- 写す処理＝`00_YFM-General/scripts/rise_rollup_crosscheck.py` の `sync_local_copy()`。
- **中身が取れなかった時は上書きしない**（関数が10個未満なら中止）。空ファイルで控えを潰さないため。
- 手で編集しても、次の晩に上書きされる。**直すのは本番（マリア）で、ここは鏡。**

## なぜここまでやるか（2026-08-14〜16 の実例）

```
8/14  ★本番で動くコードを手元に持っていなかった
      Code.gs には dailyRollup も morningCheck も無い。マリアが本番から取り直すまで動けなかった
8/16  ★控えは置いたが、更新されているかを見ていなかった
      2日前の控えを根拠に TAPZERO_LAST の仕様を説明した（結果は正しかったが、古い紙が根拠だった）
```
**控えを置くだけでは同じことが起きる。** 毎晩そろえる仕組みにして、記憶から外した。
★それでも読む前に、`Obsidian` 側の「書き出し日時」を見ること。

## 出所

```
scriptId  1t4lvCh6sR3oYHma1Z6QEjMzrB0RdruMKCnDMoChNik21fUsl2boIJzHH
明細スプシ 1vMgPbBSpPNTXiYrBCAasQlBpJAP2mO26_bblbv5ueRg（ライズ南関町_来店記録明細）
```
